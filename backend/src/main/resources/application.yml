spring:

  datasource:
    url: jdbc:postgresql://localhost:5433/syncvault
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
